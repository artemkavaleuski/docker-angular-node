version: "3.3"
services:
  server:
    build:
        context: ./server
        dockerfile: Dockerfile
    env_file:
        - ./server/.env
    ports:
        - "3000:3000"
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "8888:80"